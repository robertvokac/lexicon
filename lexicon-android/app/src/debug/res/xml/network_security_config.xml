<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only: plain HTTP to a LexiconServer on this device or on the
     development machine as the emulator sees it (10.0.2.2, and 10.0.3.2 for
     Genymotion). Every other host still requires HTTPS with a system-trusted
     certificate. Release builds use src/main/res/xml/network_security_config.xml. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
